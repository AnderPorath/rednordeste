PORT=4000

# PostgreSQL (obligatorio). En Render: DATABASE_URL desde el panel de la base de datos.
#
# Para desarrollo local (ajusta a tu DB real si corresponde):
DATABASE_URL="postgresql://localhost:5432/sistema_nordeste?schema=public"
